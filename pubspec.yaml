name: to_do_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  camera: ^0.11.0+1
  image_picker: ^1.1.2
  share_plus: ^2.0.0
  flutter:
    sdk: flutter
  path: ^1.9.0
  path_provider: ^2.1.3
  unsplash_client: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets: 
    - assets/images/
